# システム概要

生成コミット: `<hash>`

## 1. このシステムは何か

利用者が誰で、何ができるシステムかを 5〜10 行で。技術の話をここに書かない。
**非エンジニアが読んで分かる言葉で書く。**

## 2. 全体構成

Mermaid の `flowchart LR` または `flowchart TD`。

- リポジトリ / アプリケーション / DB / 外部サービスをノードにする
- 矢印には**何が流れるか**を書く。「API 呼び出し」ではなく「注文データ」
- 内部と外部の境界が分かるようにする

## 3. リポジトリと責務

| リポジトリ / パッケージ | 責務 | 主な技術 |

モノレポならワークスペースごとに書く。

## 4. 技術スタック

| 区分 | 技術 | 判定の根拠 |

区分は フロントエンド / バックエンド / ORM / DB / Router / API フレームワーク /
インフラ / CI。**判定できなかった区分は「判定できず」と書く。推測で埋めない。**

判定の根拠は manifest のファイル名と依存名。

## 5. 主要な業務フロー

このシステムで最も重要な流れを 2〜4 本、Mermaid の `sequenceDiagram` または
`flowchart` で。画面 → API → DB の縦の流れが見えるようにする。

どれが「主要」かはコードから判断する。エンドポイントの数、参照の集中、
テーブルのリレーションの中心を手がかりにする。

## 6. 資料の読み方

生成された資料の一覧と、どの資料が何に答えるか。

| 資料 | 何が分かるか |

## 7. 未確認一覧

| 対象 | 分からなかったこと | 調べた範囲 |
