# システム概要

生成コミット: `<hash>`

**この資料は全体を俯瞰するためのもの。何があるかを示す。処理の流れは書かない。**

## 1. このシステムは何か

利用者が誰で、何ができるシステムかを 5〜10 行で。技術の話をここに書かない。
**非エンジニアが読んで分かる言葉で書く。**

## 2. 全体構成

Mermaid の `flowchart LR` または `flowchart TD`。

- リポジトリ / アプリケーション / DB / 外部サービスをノードにする
- 矢印には**何が流れるか**を書く。「API 呼び出し」ではなく「注文データ」
- 内部と外部の境界が分かるようにする

## 3. リポジトリと責務

| リポジトリ / パッケージ | 責務 | 主な技術 |

モノレポならワークスペースごとに書く。

## 4. 技術スタック

| 区分 | 技術 | 判定の根拠 |

区分は フロントエンド / バックエンド / ORM / DB / Router / API フレームワーク /
インフラ / CI。**判定できなかった区分は「判定できず」と書く。推測で埋めない。**

判定の根拠は manifest のファイル名と依存名。
**ここだけはファイル名を書いてよい。** 判定の出所であって、処理を実装したファイルではない。

## 5. 資料の読み方

生成された資料の一覧と、どの資料が何に答えるか。

| 資料 | 何が分かるか |

## 6. 未確認一覧

| 対象 | 分からなかったこと | 調べた範囲 |

## 書かないこと

以下は俯瞰の邪魔になるうえ、機械照合ができない。**依頼されていない限り書かない。**

- 「主要な業務フロー」のような処理の流れの説明（散文・sequenceDiagram とも）
- 実装ファイルのパス（技術スタックの判定根拠として挙げる manifest 名を除く）
- 開発コマンド・CI の設定内容
