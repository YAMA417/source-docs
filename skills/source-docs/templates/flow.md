# 処理概要 — <機能名>

<対象リポジトリ> / コミット `<ハッシュ>` 時点

書き方は `references/flow.md`。章の順番を変えない。該当が無い章は表ごと省く。

## 1. この機能は何か

1〜2 行。誰が、どんなときに使うか。

## 2. 入力

| 項目 | 形 | 必須 | 制約 |
| --- | --- | --- | --- |
| <名前> | <型> | 要 / 任意 | <上限・形式など。無ければ「—」> |

入口が画面なら入力欄、API ならリクエストの項目を書く。

## 3. 処理の要点

1 行 1 ステップ。**5 行以内。**

1. <動詞で始める>
2. <条件があれば「〜なら」で 1 行に収める>

## 4. 出力

| 返るもの | 内容 |
| --- | --- |
| <画面表示 / レスポンス / 保存> | <何がどうなるか> |

## 5. 失敗したとき

| 起きること | 利用者から見える結果 |
| --- | --- |
| <条件> | <エラー表示・ステータスコード・遷移> |

内部でどう復旧するかは書かない。

## 6. 関連

| 種別 | 名前 |
| --- | --- |
| 画面 | <画面名> |
| API | `<METHOD /path>` |
| テーブル | `<table>` (CRUD) |
| 外部連携 | <連携先> |
| 実装 | `<path>` |

## 7. 未確認

| 対象 | 分からなかったこと | 調べた範囲 |
| --- | --- | --- |
